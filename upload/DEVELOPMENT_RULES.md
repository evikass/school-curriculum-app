# 🚨 ПРАВИЛА РАЗРАБОТКИ ИНЕТШКОЛА

## ⚠️ КРИТИЧЕСКИ ВАЖНО

### 📁 Файл `src/app/page.tsx` 
**МАКСИМАЛЬНЫЙ РАЗМЕР: 150 строк**

Если файл превышает 150 строк - **НЕМЕДЛЕННО** проводи рефакторинг!

```
page.tsx должен содержать ТОЛЬКО:
✅ Импорты компонентов
✅ Композицию (расположение компонентов)
✅ Минимум состояния (activeTab, selectedGrade)
❌ НИКАКИХ данных (schoolData - в /src/data/)
❌ НИКАКОЙ бизнес-логики (в Context или компоненты)
❌ НИКАКИХ больших JSX-структур (выносить в компоненты)
```

---

## 📂 Структура проекта

```
src/
├── app/
│   └── page.tsx          ← ТОЛЬКО композиция! (макс 150 строк)
│
├── context/
│   └── SchoolContext.tsx ← Глобальное состояние и логика
│
├── data/
│   ├── types.ts          ← TypeScript интерфейсы
│   ├── constants.ts      ← Константы (RANKS, XP_PER_LEVEL)
│   ├── school-data.tsx   ← Данные классов и предметов
│   ├── achievements.tsx  ← Достижения
│   └── daily-tasks.ts    ← Ежедневные задания
│
└── components/
    ├── ui/               ← shadcn/ui компоненты (не трогать!)
    └── school/           ← Компоненты приложения
        ├── index.ts      ← Экспорты
        ├── QuizDialog.tsx
        ├── TopicDialog.tsx
        ├── Sidebar.tsx
        └── ...
```

---

## 📏 Лимиты размеров файлов

| Файл | Макс. строк | Причина |
|------|-------------|---------|
| `page.tsx` | **150** | Главный файл - только композиция |
| `Компонент.tsx` | **300** | Если больше - разбить на подкомпоненты |
| `Context.tsx` | **400** | Если больше - вынести логику в hooks |
| `data.ts` | Без лимита | Данные могут быть большими |

---

## 🔄 Когда добавлять новую функцию

1. **Новые данные?** → `/src/data/`
2. **Новая логика состояния?** → `/src/context/SchoolContext.tsx`
3. **Новый UI элемент?** → `/src/components/school/NewComponent.tsx`
4. **Новый диалог/модал?** → Отдельный компонент + импорт в page.tsx

---

## ✅ Чек-лист перед коммитом

- [ ] `page.tsx` меньше 150 строк?
- [ ] Новые компоненты в `/components/school/`?
- [ ] Экспорт добавлен в `index.ts`?
- [ ] Данные в `/data/`, а не в компонентах?
- [ ] `bun run lint` проходит без ошибок?

---

## 📝 История рефакторингов

| Дата | Было | Стало | Причина |
|------|------|-------|---------|
| 2024-XX-XX | ~5000 строк | ~700 строк | Первый рефакторинг |
| 2025-02-27 | 7475 строк | 126 строк | Второй рефакторинг |

**НЕ ДУПАТЬ ТРЕТИЙ РАЗ!** 🛑

---

## 📚 Структура данных уроков

### 🔑 Иерархия данных (КРИТИЧЕСКИ ВАЖНО)

```
📦 Grade (Класс)                    ← Уровень 1: Класс обучения
├── id: number (0-11)
├── name: string ("1 класс")
├── shortName: string ("1 кл.")
└── subjects: Subject[]
    │
    📦 Subject (Предмет)            ← Уровень 2: Предмет
    ├── id: string ("math1")
    ├── title: string ("Математика")
    ├── icon: ReactNode
    ├── color: string
    ├── gradient: string
    ├── description: string
    └── sections: Section[]
        │
        📦 Section (Раздел)         ← Уровень 3: Раздел предмета
        ├── id: string ("math1-s1")
        ├── title: string ("Натуральные числа")
        ├── description: string
        ├── order: number
        └── topics: Topic[]
            │
            📦 Topic (Тема)         ← Уровень 4: Тема внутри раздела
            ├── id: string ("math1-s1-t1")
            ├── title: string ("Числа от 1 до 10")
            ├── description: string
            ├── theory: string (HTML)
            ├── examples: string[]
            ├── completed: boolean
            ├── difficulty: 'easy' | 'medium' | 'hard'
            ├── estimatedTime: number
            └── lessons: Lesson[]
                │
                📦 Lesson (Урок)    ← Уровень 5: Конкретный урок
                ├── id: string ("math1-s1-t1-l1")
                ├── title: string ("Введение в числа")
                ├── content: string (HTML)
                ├── videoUrl?: string
                ├── exercises: Exercise[]
                ├── completed: boolean
                └── order: number
```

### Пример полной структуры

```tsx
{
  id: 1,                          // Класс
  name: '1 класс',
  subjects: [
    {
      id: 'math1',               // Предмет
      title: 'Математика',
      sections: [
        {
          id: 'math1-s1',        // Раздел
          title: 'Натуральные числа',
          topics: [
            {
              id: 'math1-s1-t1', // Тема
              title: 'Числа от 1 до 10',
              lessons: [
                {
                  id: 'math1-s1-t1-l1',  // Урок
                  title: 'Знакомство с числами',
                  content: '...'
                }
              ]
            }
          ]
        }
      ]
    }
  ]
}
```

### Где хранить данные уроков

```
/src/data/
├── types.ts              ← TypeScript интерфейсы
├── constants.ts          ← Константы (RANKS, XP_PER_LEVEL)
├── school-data.tsx       ← Основные данные (классы 0-11)
├── preparatory-class.tsx ← Подготовительный класс (с lessons)
├── classes/              ← Данные по классам (если много контента)
│   ├── class-0.tsx       ← Подготовительный
│   ├── class-1.tsx       ← 1 класс
│   └── ...
├── subjects/             ← Данные по предметам (опционально)
│   ├── math/             ← Математика
│   │   ├── sections.tsx  ← Разделы
│   │   ├── topics.tsx    ← Темы
│   │   └── lessons.tsx   ← Уроки
│   └── ...
```

### Правила добавления контента

1. **Новый класс?** → Добавить в `schoolData` массив
2. **Новый предмет?** → Добавить в `subjects` класса
3. **Новый раздел?** → Добавить в `sections` предмета
4. **Новая тема?** → Добавить в `topics` раздела
5. **Новый урок?** → Добавить в `lessons` темы

### Шаблон раздела (Section)
```tsx
{
  id: 'math1-s1',                    // предмет-раздел-номер
  title: 'Натуральные числа',        // Название раздела
  description: 'Счёт, сравнение',    // Краткое описание
  order: 1,                          // Порядок раздела
  topics: []                         // Темы раздела
}
```

### Шаблон темы (Topic)
```tsx
{
  id: 'math1-s1-t1',                 // предмет-раздел-тема-номер
  title: 'Числа от 1 до 10',
  description: 'Краткое описание',
  theory: `<h3>Заголовок</h3>
    <p>Текст теории с HTML форматированием</p>`,
  examples: ['Пример 1', 'Пример 2'],
  completed: false,
  difficulty: 'easy',                // easy | medium | hard
  estimatedTime: 20,                 // Минуты
  lessons: []                        // Уроки темы
}
```

### Шаблон урока (Lesson)
```tsx
{
  id: 'math1-s1-t1-l1',              // предмет-раздел-тема-урок-номер
  title: 'Введение в числа',
  content: `<p>Содержимое урока...</p>`,
  videoUrl: 'https://...',            // Опционально
  exercises: [                        // Упражнения
    { type: 'choice', question: '...', options: [...] }
  ],
  completed: false,
  order: 1
}
```

### Предметы по классам (рекомендуемые)

| Класс | Предметы |
|-------|----------|
| 0 (Подг.) | Подготовка к письму, Основы счёта, Окружающий мир |
| 1-4 | Математика, Русский язык, Литература, Окружающий мир |
| 5-6 | Математика, Русский язык, Литература, История, Биология, География |
| 7-9 | Алгебра, Геометрия, Русский язык, Литература, Физика, Химия, Биология, История, География, Обществознание |
| 10-11 | Алгебра, Геометрия, Русский язык, Литература, Физика, Химия, Биология, История, Обществознание, Подготовка к ЕГЭ |

---

## 🖼️ Структура папок изображений

### Дерево папок
```
/public/images/
├── classes/          # Изображения для классов (обложки)
│   ├── class-0.svg   # Подготовительный класс
│   ├── class-1.svg   # 1 класс
│   └── ...
├── subjects/         # Изображения по предметам
│   ├── math/         # Математика
│   ├── russian/      # Русский язык
│   ├── literature/   # Литература
│   ├── english/      # Английский
│   ├── science/      # Окружающий мир
│   ├── history/      # История
│   ├── geography/    # География
│   ├── biology/      # Биология
│   ├── physics/      # Физика
│   ├── chemistry/    # Химия
│   ├── informatics/  # Информатика
│   ├── art/          # ИЗО
│   ├── music/        # Музыка
│   ├── pe/           # Физкультура
│   ├── obzh/         # ОБЖ
│   └── social/       # Обществознание
├── sections/         # Изображения для разделов (обучение, прогресс, инструменты)
├── topics/           # Изображения для тем уроков
├── lessons/          # Изображения для конкретных уроков
├── icons/            # Общие иконки приложения
├── backgrounds/      # Фоны для страниц
├── illustrations/    # Общие иллюстрации
├── achievements/     # Значки достижений
└── badges/           # Значки и ранги
```

### Правила именования файлов
```
✅ class-{номер}.svg          → class-1.svg, class-11.svg
✅ subject-{предмет}.svg      → subject-math.svg, subject-russian.svg
✅ topic-{id}.svg             → topic-math1-t1.svg
✅ achievement-{id}.svg       → achievement-first-lesson.svg
✅ badge-{name}.svg           → badge-novice.svg, badge-expert.svg
✅ icon-{name}.svg            → icon-trophy.svg, icon-star.svg
```

### Когда какие изображения использовать

| Папка | Когда использовать | Пример |
|-------|-------------------|--------|
| `classes/` | Обложки классов, выбор класса | Иконка для 5 класса |
| `subjects/` | Иконки предметов | Математика, Русский язык |
| `topics/` | Иллюстрации к темам | "Сложение", "Дроби" |
| `sections/` | Разделы приложения | "Инструменты", "Прогресс" |
| `achievements/` | Значки достижений | "Первый урок", "Стример" |
| `badges/` | Ранги и звания | "Новичок", "Легенда" |
| `icons/` | Общие иконки UI | Стрелки, чекбоксы |

### Генерация изображений
```bash
# Генерация изображения
z-ai-generate -p "описание" -o "./public/images/{папка}/{имя}.svg" -s 1024x1024

# Примеры:
z-ai-generate -p "Math icon for school app" -o "./public/images/subjects/math/icon.svg"
z-ai-generate -p "Achievement badge for first lesson" -o "./public/images/achievements/first-lesson.svg"
```

---

## 🎯 Принципы разработки

1. **Single Responsibility** - один компонент = одна задача
2. **Data-UI Separation** - данные отдельно, UI отдельно
3. **Context for State** - глобальное состояние в Context
4. **Composition over Inheritance** - компоновать, не наследовать
5. **Small Files** - маленькие файлы легче понимать и тестировать

---

*Создано: 2025-02-27*
*Последнее обновление: 2025-02-27*

---

## 👶 Детский режим (Kid-Friendly Mode)

### Автоматическое переключение режимов

Приложение автоматически переключается между двумя режимами интерфейса:

| Класс | Режим | Описание |
|-------|-------|----------|
| 0-2 | 👶 **Детский** | Большие кнопки, яркие картинки, минимум текста |
| 3-11 | 🎓 **Обычный** | Стандартный интерфейс для старшеклассников |

### Особенности детского режима

1. **KidFriendlyLessonViewer** - просмотрщик уроков для малышей:
   - Большие яркие кнопки (минимум 60px высотой)
   - Эмодзи и анимации на каждом шаге
   - Пошаговая навигация (стрелочки влево/вправо)
   - Система звёзд за прогресс
   - Празднование при завершении (конфетти, звуки)
   - Вибрация на мобильных устройствах

2. **KidGradeSelector** - выбор класса для малышей:
   - Большие карточки с эмодзи и картинками
   - Анимированные элементы
   - Цветовое кодирование классов

3. **KidSubjectCard** - карточки предметов:
   - Огромные карточки тем с картинками
   - Прогресс в виде звёзд
   - Минимум текста, максимум визуальных подсказок

### Цвета и стили для классов

```tsx
const gradeStyles = {
  0: { emoji: '🎈', color: 'from-pink-400 to-purple-400', description: 'Подготовишки' },
  1: { emoji: '🌟', color: 'from-yellow-400 to-orange-400', description: 'Первоклашки' },
  2: { emoji: '🌈', color: 'from-blue-400 to-cyan-400', description: 'Второклашки' },
  3: { emoji: '🚀', color: 'from-green-400 to-emerald-400', description: 'Третьеклашки' },
  4: { emoji: '🎯', color: 'from-purple-400 to-indigo-400', description: 'Четвероклашки' },
  // ...
}
```

### Как добавить новый детский компонент

1. Создай компонент с префиксом `Kid` (например, `KidQuizDialog`)
2. Добавь проверку `gradeId <= 2` для автоматического переключения
3. Используй большие размеры кнопок (py-6, text-lg)
4. Добавь эмодзи и анимации
5. Минимизируй текст, используй визуальные подсказки

### Файлы детского режима

```
/src/components/school/
├── KidFriendlyLessonViewer.tsx   # Просмотрщик уроков для малышей
├── GradeSelector.tsx             # Выбор класса (адаптивный)
├── SubjectGrid.tsx               # Карточки предметов (адаптивный)
└── TopicDialog.tsx               # Диалог темы (адаптивный)
```
