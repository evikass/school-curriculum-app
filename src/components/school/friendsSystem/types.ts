export interface Friend {
  id: string
  name: string
  avatar: string
  points: number
  stars: number
  streak: number
  grade: number
  lastActive: string
  isOnline: boolean
}

export interface SearchResult {
  id: string
  name: string
  avatar: string
  grade: number
}
